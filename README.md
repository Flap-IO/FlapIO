# FlapIO
FlapIO is a reinforcement learning agent being built to learn Flappy Bird entirely from scratch — no hardcoded rules, no human demonstrations. The plan: build a custom Flappy Bird clone in Pygame, wrap it as a Gymnasium environment, then train a PPO agent (via Stable-Baselines3) against it using a shaped reward signal.
